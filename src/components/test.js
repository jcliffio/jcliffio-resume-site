var React = require('react'),
    Skills = require('./Skills.js');

var test = React.createClass({
  render () {
    return (
      <div>
        <Skills />
      </div>
    );
  }
});

module.exports = test;
